export interface API {
  productId: string;
  userId: string;
  reviewId: string;
  qnaId: string;
  cartId: string;
  categoryId: string;
  regionId: string;
  page: number;
  body: string;
  order: boolean;
}
